from flask import Flask, request
import requests
from random import choice

app = Flask(__name__)

@app.route('/', methods=['GET', 'POST'])
def home():
    titles = []
    character =
    combat =
    modifier = 
    return